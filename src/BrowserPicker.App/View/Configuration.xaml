<UserControl 
	x:Class="BrowserPicker.View.Configuration"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:converter="clr-namespace:BrowserPicker.Converter"
	xmlns:view="clr-namespace:BrowserPicker.View"
	xmlns:viewModel="clr-namespace:BrowserPicker.ViewModel"
	xmlns:browserPicker="clr-namespace:BrowserPicker;assembly=BrowserPicker.Common"
	xmlns:system="clr-namespace:System;assembly=mscorlib"
	mc:Ignorable="d"
	d:DataContext="{d:DesignInstance viewModel:ConfigurationViewModel,d:IsDesignTimeCreatable=True}"
	Height="400" Width="600"
	Background="#333">
	<UserControl.Resources>
		<ResourceDictionary>
			<converter:IconFileToImageConverter x:Key="IconConverter" />

			<ObjectDataProvider x:Key="MatchTypes" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
				<ObjectDataProvider.MethodParameters>
					<x:Type TypeName="browserPicker:MatchType"/>
				</ObjectDataProvider.MethodParameters>
			</ObjectDataProvider>

			<DataTemplate x:Key="BrowserIcon" DataType="{x:Type viewModel:BrowserViewModel}">
				<Image Margin="0,0,0,0" DataContext="{Binding Model.IconPath, Converter={StaticResource IconConverter}}" d:DataContext="{DynamicResource DefaultIcon}">
					<Image.Style>
						<Style TargetType="{x:Type Image}">
							<Setter Property="Source" Value="{Binding}" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding}" Value="{x:Null}">
									<Setter Property="Source" Value="{DynamicResource DefaultIcon}" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Image.Style>
				</Image>
			</DataTemplate>

			<DataTemplate x:Key="BrowserIconAndName" DataType="viewModel:BrowserViewModel">
				<StackPanel Orientation="Horizontal">
					<ContentPresenter ContentTemplate="{StaticResource BrowserIcon}" VerticalAlignment="Center" />
					<TextBlock Padding="5,0,0,0" Text="{Binding Model.Name}" VerticalAlignment="Center" />
				</StackPanel>
			</DataTemplate>
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>

		<TabControl Background="#77000000" BorderBrush="#77FFFFFF" VerticalAlignment="Stretch">
			<TabControl.Style>
				<Style TargetType="TabControl">
					<Setter Property="SelectedIndex" Value="1" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding Welcome}" Value="True">
							<Setter Property="SelectedIndex" Value="0" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</TabControl.Style>
			
			<TabControl.Resources>
				<Style TargetType="{x:Type TabItem}">
					<Setter Property="Template">
						<Setter.Value>
							<ControlTemplate TargetType="{x:Type TabItem}">
								<Border Name="Border" Background="#22000000" Margin="2,0" BorderBrush="#77FFFFFF" BorderThickness="1,1,1,0" CornerRadius="10,10,0,0" TextBlock.Foreground="White" TextBlock.FontSize="12">
									<ContentPresenter x:Name="ContentSite" ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,2" RecognizesAccessKey="True" />
								</Border>
								<ControlTemplate.Triggers>
									<Trigger Property="IsSelected" Value="True">
										<Setter Property="FontWeight" Value="Bold" />
										<Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
									</Trigger>
									<Trigger Property="IsMouseOver" Value="true">
										<Setter Property="FontWeight" Value="Bold" />
									</Trigger>
								</ControlTemplate.Triggers>
							</ControlTemplate>
						</Setter.Value>
					</Setter>
				</Style>
			</TabControl.Resources>

			<TabItem Header="欢迎">
				<TabItem.Style>
					<Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding Welcome}" Value="True">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TabItem.Style>
				<TextBlock Foreground="White" Margin="4" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Light">
					欢迎使用浏览器选择器，希望你喜欢这个应用！<LineBreak />
					我已经打开了设置窗口，<LineBreak />
					这样你就可以开始前先看看周围啦 :)
					<LineBreak />
					<LineBreak />
					请在 GitHub 上给它点个 star ⭐，这样当有新版本发布时你就会收到通知。
					<LineBreak />
					<LineBreak />
					如果您遇到任何问题或有了什么想法，<LineBreak />
					请访问 <Hyperlink Foreground="LightBlue">github.com/XTsat/BrowserPicker_zh</Hyperlink> 提交问题！<LineBreak />
					<LineBreak />
					回头见！
					<LineBreak />
					<LineBreak />
					<Run FontSize="8">这条消息将在 10 秒后自动销毁</Run>
				</TextBlock>
			</TabItem>

			<TabItem Header="浏览器">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<ScrollViewer VerticalScrollBarVisibility="Auto">
						<ItemsControl ItemsSource="{Binding ParentViewModel.Choices}">
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
									<StackPanel Orientation="Vertical" CanVerticallyScroll="True" />
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
							<ItemsControl.ItemTemplate>
								<DataTemplate DataType="viewModel:BrowserViewModel">
									<Grid>
										<Grid.Style>
											<Style>
												<Setter Property="UIElement.Visibility" Value="Visible" />
												<Style.Triggers>
													<DataTrigger Binding="{Binding Model.Removed}" Value="True">
														<Setter Property="UIElement.Visibility" Value="Collapsed" />
													</DataTrigger>
												</Style.Triggers>
											</Style>
										</Grid.Style>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="Auto" />
											<ColumnDefinition Width="*" />
											<ColumnDefinition Width="Auto" />
											<ColumnDefinition Width="Auto" />
											<ColumnDefinition Width="Auto" />
											<ColumnDefinition Width="Auto" />
										</Grid.ColumnDefinitions>
										<ContentPresenter Content="{Binding}" ContentTemplate="{StaticResource BrowserIcon}" Height="40" Width="40" Margin="20,0,0,0" />
										<TextBlock Text="{Binding Model.Name}" Grid.Column="1" Foreground="White" Margin="10" FontSize="18" />
										<Grid Grid.Column="2" VerticalAlignment="Center">
											<Grid.Style>
												<Style TargetType="Grid">
													<Style.Triggers>
														<DataTrigger Binding="{Binding IsManuallyOrdered}" Value="False">
															<Setter Property="Visibility" Value="Collapsed" />
														</DataTrigger>
													</Style.Triggers>
												</Style>
											</Grid.Style>
											<Grid.RowDefinitions>
												<RowDefinition Height="Auto" />
												<RowDefinition Height="Auto" />
											</Grid.RowDefinitions>
											<TextBlock FontSize="10"><Hyperlink TextDecorations="None" Command="{Binding MoveUp}">🔼</Hyperlink></TextBlock>
											<TextBlock FontSize="10" Grid.Row="1"><Hyperlink TextDecorations="None" Command="{Binding MoveDown}">🔽</Hyperlink></TextBlock>
										</Grid>
										<TextBlock Grid.Column="3" Margin="10" FontSize="16">
										<Hyperlink Command="{Binding Disable}">
											<TextBlock>
													<TextBlock.Style>
														<Style>
															<Setter Property="TextBlock.Foreground" Value="Green" />
															<Setter Property="TextBlock.Text" Value="启用" />
															<Style.Triggers>
																<DataTrigger Binding="{Binding Model.Disabled}" Value="True">
																	<Setter Property="TextBlock.Foreground" Value="OrangeRed" />
																	<Setter Property="TextBlock.Text" Value="禁用" />
																</DataTrigger>
															</Style.Triggers>
														</Style>
													</TextBlock.Style>
											</TextBlock>
										</Hyperlink>
									</TextBlock>
										<TextBlock Grid.Column="4" Margin="10" FontSize="16">
										<Hyperlink Command="{Binding Edit}">
											<TextBlock Text="编辑" Foreground="CadetBlue" ToolTip="编辑浏览器定义" />
										</Hyperlink>
									</TextBlock>
										<TextBlock Grid.Column="5" Margin="10" FontSize="16">
										<Hyperlink Command="{Binding Remove}">
											<TextBlock Text="X" Foreground="Red" ToolTip="从列表中移除浏览器" />
										</Hyperlink>
									</TextBlock>
									</Grid>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</ScrollViewer>
					<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4">
						<TextBlock>
							<Hyperlink Command="{Binding AddBrowser}" Foreground="White">
								<TextBlock Text="添加浏览器" />
							</Hyperlink>
						</TextBlock>

						<TextBlock Margin="10,0,0,0">
							<Hyperlink Command="{Binding RefreshBrowsers}" Foreground="White">
								<TextBlock Text="刷新浏览器列表" />
							</Hyperlink>
						</TextBlock>
					</StackPanel>
				</Grid>
			</TabItem>

			<TabItem Header="行为">
				<StackPanel Orientation="Vertical">
					<StackPanel.Resources>
						<Style TargetType="{x:Type CheckBox}">
							<Setter Property="Margin" Value="5,0,0,5"/>
						</Style>
					</StackPanel.Resources>

					<CheckBox Content="使用纯黑背景" IsChecked="{Binding Settings.DisableTransparency}" Foreground="White" />

					<CheckBox Content="始终显示浏览器选择窗口" IsChecked="{Binding Settings.AlwaysPrompt}" Foreground="White" />

					<StackPanel Orientation="Horizontal">
						<CheckBox Content="当无匹配链接的默认浏览器时，使用：" IsChecked="{Binding Settings.UseFallbackDefault}" Foreground="White" />
						<ComboBox
							ItemsSource="{Binding ParentViewModel.Choices}"
							ItemTemplate="{StaticResource BrowserIconAndName}"
							IsSynchronizedWithCurrentItem="False"
							SelectedValuePath="Model.Name"
							SelectedValue="{Binding Settings.DefaultBrowser}"
							Height="22" Margin="5,-4,0,0" />
					</StackPanel>

					<CheckBox Content="无匹配默认浏览器时始终询问" IsChecked="{Binding Settings.AlwaysAskWithoutDefault}" Foreground="White" />

					<CheckBox Content="即使浏览器未运行也使用默认设置" IsChecked="{Binding Settings.AlwaysUseDefaults}" Foreground="White" Checked="CheckBox_Checked" />

					<RadioButton GroupName="OrderBy" Content="手动排序" IsChecked="{Binding Settings.UseManualOrdering}" Foreground="White" Margin="6,2" />
					<RadioButton GroupName="OrderBy" Content="根据使用情况排序" IsChecked="{Binding Settings.UseAutomaticOrdering}" Foreground="White" Margin="6,2" />
					<RadioButton GroupName="OrderBy" Content="按字母顺序排序" IsChecked="{Binding Settings.UseAlphabeticalOrdering}" Foreground="White" Margin="6,2,4,6" />

					<CheckBox Content="禁用链接解析" Foreground="White" IsChecked="{Binding Settings.DisableNetworkAccess}" ToolTip="Disabled url resolution feature" />

					<StackPanel Margin="25,0,0,0">
						<StackPanel.Style>
							<Style>
								<Setter Property="Control.IsEnabled" Value="True" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding Settings.DisableNetworkAccess}" Value="True">
										<Setter Property="Control.IsEnabled" Value="False" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</StackPanel.Style>
						<StackPanel Orientation="Horizontal">
							<TextBlock Foreground="White" Text="链接解析超时时间(毫秒)：" />
							<TextBox Text="{Binding Settings.UrlLookupTimeoutMilliseconds, UpdateSourceTrigger=PropertyChanged}"  />
						</StackPanel>
					</StackPanel>
				</StackPanel>
			</TabItem>

			<TabItem Header="默认值">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<TextBlock Text="按域名设置默认浏览器" Foreground="White" Margin="5" />
					<DataGrid
						ItemsSource="{Binding Defaults}"
						Grid.Row="1"
						AutoGenerateColumns="False"
						RowHeight="32"
						Background="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"
						BorderBrush="{x:Null}"
						CanUserDeleteRows="False"
						CanUserAddRows="False"
						CanUserReorderColumns="False"
						CanUserResizeColumns="False">
						<DataGrid.Columns>
							<DataGridTemplateColumn Header="匹配              ">
								<DataGridTemplateColumn.CellTemplate>
									<DataTemplate DataType="browserPicker:DefaultSetting">
										<ComboBox
											ItemsSource="{Binding Source={StaticResource MatchTypes}}"
											IsSynchronizedWithCurrentItem="False"
											SelectedValue="{Binding Type}"
											VerticalContentAlignment="Center" />
									</DataTemplate>
								</DataGridTemplateColumn.CellTemplate>
							</DataGridTemplateColumn>
							<DataGridTextColumn Header="模式" Binding="{Binding Pattern}" Width="*">
								<DataGridTextColumn.ElementStyle>
									<Style TargetType="TextBlock">
										<Setter Property="VerticalAlignment" Value="Center" />
										<Setter Property="Padding" Value="5,0" />
									</Style>
								</DataGridTextColumn.ElementStyle>
							</DataGridTextColumn>
							<DataGridTemplateColumn Header="浏览器" Width="55" SortMemberPath="Model.Name">
								<DataGridTemplateColumn.CellTemplate>
									<DataTemplate DataType="browserPicker:DefaultSetting">
										<ComboBox
											ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}, Path=DataContext.ParentViewModel.Choices}"
											ItemTemplate="{StaticResource BrowserIcon}"
											IsSynchronizedWithCurrentItem="False"
											SelectedValue="{Binding Browser}"
											SelectedValuePath="Model.Name"
											VerticalContentAlignment="Center" />
									</DataTemplate>
								</DataGridTemplateColumn.CellTemplate>
							</DataGridTemplateColumn>
							<DataGridTemplateColumn>
								<DataGridTemplateColumn.CellTemplate>
									<DataTemplate DataType="browserPicker:DefaultSetting">
										<Button Content="X" Command="{Binding Remove}" />
									</DataTemplate>
								</DataGridTemplateColumn.CellTemplate>
							</DataGridTemplateColumn>
						</DataGrid.Columns>
					</DataGrid>

					<Grid Grid.Row="2">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<ComboBox ItemsSource="{Binding Source={StaticResource MatchTypes}}" SelectedItem="{Binding NewDefaultMatchType}" />
						<TextBox Text="{Binding NewDefaultPattern}" Grid.Column="1" />
						<ComboBox
							ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=view:Configuration}, Path=DataContext.ParentViewModel.Choices}"
							ItemTemplate="{StaticResource BrowserIcon}"
							IsSynchronizedWithCurrentItem="False"
							SelectedValue="{Binding NewDefaultBrowser}"
							SelectedValuePath="Model.Name"
							Grid.Column="2" Height="22" />
						<Button Content="添加" Grid.Column="3" Padding="5,0" Command="{Binding AddDefault}" />
					</Grid>
				</Grid>
			</TabItem>

			<TabItem Header="测试链接">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<TextBlock Text="链接：" Foreground="White" />

					<TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding TestDefaultsURL, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Text="默认浏览器匹配：" Grid.Column="0" Grid.Row="2" Foreground="White" />
					<TextBlock Text="{Binding TestDefaultsResult}" Grid.Column="1" Grid.Row="2" Foreground="LightGreen" />

					<TextBlock Text="当前浏览器选择：" Grid.Column="0" Grid.Row="3" Foreground="White" />
					<TextBlock Text="{Binding TestActualResult}" Grid.Column="1" Grid.Row="3" Foreground="LightGreen" />
				</Grid>
			</TabItem>

			<TabItem Header="备份">
				<Grid Margin="10" VerticalAlignment="Top">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<TextBlock Text="备份或恢复您的 BrowserPicker 配置：" Foreground="White" Margin="5" />
					<Button Content="导出设置" Grid.Row="1" Grid.Column="0" Margin="5" Padding="5" Command="{Binding Backup}" />
					<Button Content="导入设置" Grid.Row="2" Grid.Column="0" Margin="5" Padding="5" Command="{Binding Restore}" />
					<ScrollViewer Grid.Row="3" Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="5">
						<TextBlock Text="{Binding Settings.BackupLog}" Foreground="White" TextWrapping="Wrap" />
					</ScrollViewer>
				</Grid>
			</TabItem>

			<TabItem Header="URL 缩短器">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition />
						<ColumnDefinition />
					</Grid.ColumnDefinitions>
					<TextBlock Foreground="White" Text="这些域名是已知的网址缩短服务，除非通过设置禁止，否则会在启动时进行探测。" Grid.ColumnSpan="2" />

					<TextBlock Foreground="White" Text="默认缩短工具" Grid.Row="1" Grid.Column="0" Margin="5" />
					<ListBox Grid.Row="2" Grid.Column="0" ItemsSource="{Binding DefaultUrlShorteners}" SelectionMode="Single" />
					<TextBlock Grid.Row="3" Grid.Column="0" FontSize="10" Text="ℹ️ 以上未显示一些额外的跳转页面" ToolTip="有些页面（例如 Outlook 安全链接）有特殊的处理方式，目标 URL 位于查询字符串中，这些页面未列入此列表，因为它们需要在代码中提供额外的信息。" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" />

					<TextBlock Foreground="White" Text="其他缩短工具" Grid.Row="1" Grid.Column="1" Margin="5" />
					<ListBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AdditionalUrlShorteners}" x:Name="UrlShorteners" SelectionMode="Single" />
					
					<Grid Grid.Row="3" Grid.Column="1">
						<Grid.ColumnDefinitions>
							<ColumnDefinition />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<TextBox Text="{Binding NewUrlShortener}" x:Name="NewShortener" />
						<Button Grid.Column="1" Grid.Row="0" Content="添加" Padding="10,0" Command="{Binding AddShortener}" CommandParameter="{Binding Text, ElementName=NewShortener}" />
						<Button Grid.Column="2" Grid.Row="0" Content="移除" Padding="10,0" Command="{Binding RemoveShortener}" CommandParameter="{Binding SelectedItem, ElementName=UrlShorteners}" />
					</Grid>
				</Grid>
			</TabItem>
			
		</TabControl>

		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">

			<TextBlock Padding="4">
				<TextBlock.Style>
					<Style>
						<Style.Triggers>
							<DataTrigger Binding="{Binding ParentViewModel.Url.TargetURL}" Value="{x:Null}">
								<Setter Property="UIElement.Visibility" Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TextBlock.Style>
				<Hyperlink Command="{Binding ParentViewModel.Configure}" Foreground="White">
					<TextBlock Text="关闭" />
				</Hyperlink>
			</TextBlock>

			<TextBlock Padding="4">
				<TextBlock.Style>
					<Style>
						<Setter Property="UIElement.Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding ParentViewModel.Url.TargetURL}" Value="{x:Null}">
								<Setter Property="UIElement.Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TextBlock.Style>
				<Hyperlink Command="{Binding ParentViewModel.Exit}" Foreground="White">
					<TextBlock Text="退出程序" />
				</Hyperlink>
			</TextBlock>

		</StackPanel>

	</Grid>
</UserControl>
