<Window
	x:Class="BrowserPicker.UI.Views.LoadingWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ui="clr-namespace:BrowserPicker.UI"
	xmlns:viewModels="clr-namespace:BrowserPicker.UI.ViewModels"
	Title="Browser Picker"
	d:DataContext="{d:DesignInstance viewModels:ApplicationViewModel,
									 d:IsDesignTimeCreatable=true}"
	AllowsTransparency="True"
	Background="Transparent"
	ResizeMode="NoResize"
	ShowInTaskbar="False"
	SizeChanged="Window_SizeChanged"
	SizeToContent="WidthAndHeight"
	WindowStartupLocation="CenterScreen"
	WindowStyle="None"
	mc:Ignorable="d">
	<Border Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" BorderBrush="#22FFFFFF" BorderThickness="2" CornerRadius="5">
		<Border.Style>
			<Style TargetType="{x:Type Border}">
				<Setter Property="Opacity" Value="1" />
				<Style.Triggers>
					<DataTrigger Binding="{Binding DisableTransparency, Source={x:Static ui:App.Settings}}" Value="False">
						<Setter Property="Opacity" Value="{Binding WindowOpacity, Source={x:Static ui:App.Settings}}" />
					</DataTrigger>
				</Style.Triggers>
			</Style>
		</Border.Style>
		<Grid Margin="10" TextElement.Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<TextBlock Margin="20" FontSize="18">Scanning URL...</TextBlock>
			<TextBlock
				Grid.Row="1"
				MaxWidth="380"
				Margin="5"
				Text="{Binding Url.UnderlyingTargetURL}"
				TextTrimming="CharacterEllipsis"
				TextWrapping="WrapWithOverflow" />
		</Grid>
	</Border>
</Window>
