<Window
	x:Class="BrowserPicker.UI.Views.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ui="clr-namespace:BrowserPicker.UI"
	xmlns:viewModels="clr-namespace:BrowserPicker.UI.ViewModels"
	xmlns:views="clr-namespace:BrowserPicker.UI.Views"
	Title="Browser Picker"
	MinWidth="430"
	MinHeight="200"
	d:DataContext="{d:DesignInstance viewModels:ApplicationViewModel,
									 d:IsDesignTimeCreatable=true}"
	AllowsTransparency="True"
	Background="Transparent"
	Closing="MainWindow_Closing"
	ContentRendered="MainWindow_ContentRendered"
	KeyDown="MainWindow_OnKeyDown"
	Loaded="MainWindow_Loaded"
	PreviewKeyUp="MainWindow_OnPreviewKeyUp"
	ResizeMode="CanResize"
	ShowInTaskbar="False"
	SizeChanged="Window_SizeChanged"
	SizeToContent="WidthAndHeight"
	Topmost="True"
	WindowStartupLocation="CenterScreen"
	WindowState="Normal"
	WindowStyle="None"
	mc:Ignorable="d">
	<Window.InputBindings>
		<KeyBinding Command="{Binding CopyUrl}" Gesture="Ctrl+C" />
	</Window.InputBindings>
	<Border CornerRadius="5">
		<Border.Style>
			<Style TargetType="{x:Type Border}">
				<Setter Property="Background" Value="Transparent" />
				<Style.Triggers>
					<DataTrigger Binding="{Binding Configuration.Settings.DisableTransparency}" Value="True">
						<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" />
					</DataTrigger>
				</Style.Triggers>
			</Style>
		</Border.Style>
		<Border
			Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}"
			BorderBrush="#22FFFFFF"
			BorderThickness="2"
			CornerRadius="5"
			MouseLeftButtonDown="DragWindow">
			<Border.Style>
				<Style TargetType="{x:Type Border}">
					<Setter Property="Opacity" Value="1" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding Configuration.Settings.DisableTransparency}" Value="False">
							<Setter Property="Opacity" Value="{Binding Configuration.Settings.WindowOpacity}" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Border.Style>
			<Grid TextElement.FontSize="{Binding Configuration.Settings.FontSize, FallbackValue=14}" TextElement.Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}">
				<Grid Margin="10">
					<Grid.RowDefinitions>
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<Border x:Name="PickerHost" Grid.Row="0" Grid.Column="0">
						<Border.Style>
							<Style>
								<Setter Property="UIElement.Visibility" Value="Visible" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding ConfigurationMode}" Value="True">
										<Setter Property="UIElement.Visibility" Value="Collapsed" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Border.Style>
						<views:BrowserList />
					</Border>
					<Border Grid.Row="0" Grid.Column="0">
						<Border.Style>
							<Style>
								<Setter Property="UIElement.Visibility" Value="Collapsed" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding ConfigurationMode}" Value="True">
										<Setter Property="UIElement.Visibility" Value="Visible" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Border.Style>
						<views:Configuration DataContext="{Binding Configuration}" />
					</Border>
					<Canvas
						Grid.Row="0"
						Grid.Column="0"
						Width="14"
						Height="14"
						HorizontalAlignment="Right"
						VerticalAlignment="Bottom"
						Background="Transparent"
						Cursor="SizeNWSE"
						PreviewMouseLeftButtonDown="ResizeGrip_PreviewMouseLeftButtonDown">
						<!--  Two diagonal lines for grip (⌟), theme-aware  -->
						<Line
							Opacity="0.6"
							Stroke="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}"
							StrokeThickness="1.5"
							X1="4"
							X2="14"
							Y1="14"
							Y2="4" />
						<Line
							Opacity="0.6"
							Stroke="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}"
							StrokeThickness="1.5"
							X1="9"
							X2="14"
							Y1="14"
							Y2="9" />
					</Canvas>
				</Grid>
				<Button
					Width="50"
					Height="40"
					Padding="0,2,0,0"
					HorizontalAlignment="Right"
					VerticalAlignment="Top"
					HorizontalContentAlignment="Center"
					VerticalContentAlignment="Center"
					Background="Transparent"
					BorderBrush="Transparent"
					BorderThickness="0"
					Command="{Binding Exit}"
					Content="&#xE8BB;"
					Cursor="Hand"
					FontFamily="Segoe MDL2 Assets"
					FontSize="10"
					FontWeight="Normal"
					Foreground="Silver"
					ToolTip="Close Browser Picker" />
			</Grid>
		</Border>
	</Border>
</Window>
