<UserControl
	x:Class="BrowserPicker.UI.Views.Configuration"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:common="clr-namespace:BrowserPicker.Common;assembly=BrowserPicker.Common"
	xmlns:converters="clr-namespace:BrowserPicker.UI.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:logging="clr-namespace:Microsoft.Extensions.Logging;assembly=Microsoft.Extensions.Logging.Abstractions"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:securityProfiles="clr-namespace:BrowserPicker.UI.SecurityProfiles"
	xmlns:system="clr-namespace:System;assembly=mscorlib"
	xmlns:ui="clr-namespace:BrowserPicker.UI"
	xmlns:viewModels="clr-namespace:BrowserPicker.UI.ViewModels"
	xmlns:views="clr-namespace:BrowserPicker.UI.Views"
	x:Name="Root"
	HorizontalAlignment="Stretch"
	VerticalAlignment="Stretch"
	d:DataContext="{d:DesignInstance viewModels:ConfigurationViewModel,
									 d:IsDesignTimeCreatable=True}"
	d:Height="400"
	d:Width="600"
	mc:Ignorable="d">
	<UserControl.Resources>
		<ResourceDictionary>
			<!--  Tabs: visible header with border/background, compact, selected tab joins content area.  -->
			<Style BasedOn="{StaticResource {x:Type TabItem}}" TargetType="TabItem">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="Padding" Value="8,4" />
				<Setter Property="Margin" Value="0,0,1,0" />
				<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" />
				<Setter Property="BorderBrush" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="BorderThickness" Value="1,1,1,0" />
				<Setter Property="Opacity" Value="0.72" />
				<Setter Property="FontWeight" Value="Normal" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="TabItem">
							<Border
								x:Name="Border"
								Margin="{TemplateBinding Margin}"
								Padding="{TemplateBinding Padding}"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								CornerRadius="3,3,0,0">
								<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header" />
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsSelected" Value="True">
									<Setter TargetName="Border" Property="Panel.ZIndex" Value="1" />
									<Setter TargetName="Border" Property="Margin" Value="0,0,1,-1" />
									<Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0" />
									<Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundSemiTransparentBrushKey}}" />
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
				<Style.Triggers>
					<Trigger Property="IsSelected" Value="True">
						<Setter Property="Panel.ZIndex" Value="1" />
						<Setter Property="Margin" Value="0,0,1,-1" />
						<Setter Property="Opacity" Value="1" />
						<Setter Property="FontWeight" Value="SemiBold" />
					</Trigger>
				</Style.Triggers>
			</Style>
			<Style BasedOn="{StaticResource {x:Type TabControl}}" TargetType="TabControl">
				<Setter Property="Padding" Value="4,0,4,4" />
			</Style>
			<Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<Style BasedOn="{StaticResource {x:Type RadioButton}}" TargetType="RadioButton">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<Style BasedOn="{StaticResource {x:Type Hyperlink}}" TargetType="{x:Type Hyperlink}">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<Style x:Key="BrowserComboBoxStyle" BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="ComboBox">
				<Setter Property="MinHeight" Value="32" />
				<Setter Property="VerticalContentAlignment" Value="Center" />
				<Setter Property="Padding" Value="2" />
			</Style>
			<Style x:Key="DefaultsGridComboBoxStyle" BasedOn="{StaticResource BrowserComboBoxStyle}" TargetType="ComboBox">
				<Setter Property="MinHeight" Value="28" />
				<Setter Property="Padding" Value="5,0,28,0" />
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="Background" Value="Transparent" />
				<Setter Property="BorderBrush" Value="Transparent" />
				<Setter Property="BorderThickness" Value="0" />
			</Style>
			<Style x:Key="DefaultsGridEditorTextBoxStyle" BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
				<Setter Property="Padding" Value="5,0" />
				<Setter Property="VerticalContentAlignment" Value="Center" />
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="Background" Value="Transparent" />
				<Setter Property="BorderBrush" Value="Transparent" />
				<Setter Property="BorderThickness" Value="0" />
				<Setter Property="CaretBrush" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<converters:ComboBoxBrowserItemFlagConverter x:Key="ComboBoxBrowserItemFlagConverter" />
			<converters:SecurityProfileSelectionConverter x:Key="SecurityProfileSelectionConverter" />
			<Style x:Key="VisibleBrowserComboItemStyle" BasedOn="{StaticResource {x:Type ComboBoxItem}}" TargetType="ComboBoxItem">
				<Style.Triggers>
					<DataTrigger Binding="{Binding Content, RelativeSource={RelativeSource Self}, Converter={StaticResource ComboBoxBrowserItemFlagConverter}, ConverterParameter=Disabled}" Value="True">
						<Setter Property="Visibility" Value="Collapsed" />
					</DataTrigger>
					<DataTrigger Binding="{Binding Content, RelativeSource={RelativeSource Self}, Converter={StaticResource ComboBoxBrowserItemFlagConverter}, ConverterParameter=Removed}" Value="True">
						<Setter Property="Visibility" Value="Collapsed" />
					</DataTrigger>
				</Style.Triggers>
			</Style>

			<converters:IconFileToImageConverter x:Key="IconConverter" />

			<ObjectDataProvider x:Key="ThemeModes" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
				<ObjectDataProvider.MethodParameters>
					<x:Type TypeName="common:ThemeMode" />
				</ObjectDataProvider.MethodParameters>
			</ObjectDataProvider>

			<DataTemplate x:Key="BrowserIcon" DataType="{x:Type viewModels:BrowserViewModel}">
				<Image Margin="0,0,0,0" d:DataContext="{DynamicResource DefaultIcon}" DataContext="{Binding Model.IconPath, Converter={StaticResource IconConverter}}">
					<Image.Style>
						<Style TargetType="{x:Type Image}">
							<Setter Property="Source" Value="{Binding}" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding}" Value="{x:Null}">
									<Setter Property="Source" Value="{DynamicResource DefaultIcon}" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Image.Style>
				</Image>
			</DataTemplate>

			<DataTemplate x:Key="BrowserIconAndName" DataType="viewModels:BrowserViewModel">
				<StackPanel Orientation="Horizontal">
					<ContentPresenter VerticalAlignment="Center" ContentTemplate="{StaticResource BrowserIcon}" />
					<TextBlock Padding="5,0,0,0" VerticalAlignment="Center" Text="{Binding Model.Name}" />
				</StackPanel>
			</DataTemplate>
			<!--  Icon + name for use in compact dropdowns (add-default row, etc.) with a visible icon size  -->
			<DataTemplate x:Key="BrowserIconAndNameCombo" DataType="viewModels:BrowserViewModel">
				<Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Image
						Grid.Column="0"
						Width="16"
						Height="16"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"
						d:DataContext="{DynamicResource DefaultIcon}"
						DataContext="{Binding Model.IconPath, Converter={StaticResource IconConverter}}"
						Stretch="Uniform">
						<Image.Style>
							<Style TargetType="{x:Type Image}">
								<Setter Property="Source" Value="{Binding}" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding}" Value="{x:Null}">
										<Setter Property="Source" Value="{DynamicResource DefaultIcon}" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Image.Style>
					</Image>
					<TextBlock Grid.Column="1" Margin="6,0,0,0" VerticalAlignment="Center" Text="{Binding Model.Name}">
						<TextBlock.Style>
							<Style TargetType="TextBlock">
								<Style.Triggers>
									<DataTrigger Binding="{Binding Model.Disabled}" Value="True">
										<Setter Property="Opacity" Value="0.7" />
										<Setter Property="TextDecorations" Value="Strikethrough" />
									</DataTrigger>
									<DataTrigger Binding="{Binding Model.Removed}" Value="True">
										<Setter Property="Opacity" Value="0.7" />
										<Setter Property="TextDecorations" Value="Strikethrough" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</TextBlock.Style>
					</TextBlock>
				</Grid>
			</DataTemplate>
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" TextElement.Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>

		<TabControl
			x:Name="Tabs"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}"
			SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
			SelectionChanged="TabControl_SelectionChanged">

			<TabItem Header="Welcome">
				<TabItem.Style>
					<Style BasedOn="{StaticResource {x:Type TabItem}}" TargetType="TabItem">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding Welcome}" Value="True">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TabItem.Style>
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<TextBlock
						Margin="4"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"
						FontSize="16"
						FontWeight="Light"
						TextWrapping="Wrap">
						Welcome to Browser Picker, hope you enjoy my little app!<LineBreak />
						I've gone ahead and opened up the settings window,<LineBreak />
						so you can have a look around before you get started :)<LineBreak />
						<LineBreak />
						Please give it a ⭐ on GitHub, so you will get notified when a new release is made.<LineBreak />
						<LineBreak />
						Should you run into any kind of trouble or get an idea,<LineBreak />
						please visit<Hyperlink>github.com/mortenn/BrowserPicker</Hyperlink>
						and file an issue!<LineBreak />
						<LineBreak />
						Be seeing you!<LineBreak />
						<LineBreak />
						<Run FontSize="8">This message will self-destruct in 10 seconds</Run>
					</TextBlock>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Browsers">
				<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<ScrollViewer VerticalScrollBarVisibility="Auto">
							<ItemsControl d:ItemsSource="{Binding ParentViewModel.Choices}" ItemsSource="{Binding ParentViewModel.Choices}">
								<ItemsControl.ItemsPanel>
									<ItemsPanelTemplate>
										<StackPanel CanVerticallyScroll="True" Orientation="Vertical" />
									</ItemsPanelTemplate>
								</ItemsControl.ItemsPanel>
								<ItemsControl.ItemTemplate>
									<DataTemplate DataType="viewModels:BrowserViewModel">
										<Grid>
											<Grid.Style>
												<Style>
													<Setter Property="UIElement.Visibility" Value="Visible" />
													<Style.Triggers>
														<DataTrigger Binding="{Binding Model.Removed}" Value="True">
															<Setter Property="UIElement.Visibility" Value="Collapsed" />
														</DataTrigger>
													</Style.Triggers>
												</Style>
											</Grid.Style>
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="Auto" />
												<ColumnDefinition Width="*" />
												<ColumnDefinition Width="Auto" />
												<ColumnDefinition Width="Auto" />
												<ColumnDefinition Width="Auto" />
												<ColumnDefinition Width="Auto" />
												<ColumnDefinition Width="Auto" />
											</Grid.ColumnDefinitions>
											<ContentPresenter
												Width="40"
												Height="40"
												Margin="20,0,0,0"
												Content="{Binding}"
												ContentTemplate="{StaticResource BrowserIcon}" />
											<TextBlock Grid.Column="1" Margin="10" FontSize="18" Text="{Binding Model.Name}" />
											<Grid Grid.Column="2" VerticalAlignment="Center">
												<Grid.Style>
													<Style TargetType="Grid">
														<Style.Triggers>
															<DataTrigger Binding="{Binding IsManuallyOrdered}" Value="False">
																<Setter Property="Visibility" Value="Collapsed" />
															</DataTrigger>
														</Style.Triggers>
													</Style>
												</Grid.Style>
												<Grid.RowDefinitions>
													<RowDefinition Height="Auto" />
													<RowDefinition Height="Auto" />
												</Grid.RowDefinitions>
												<TextBlock FontSize="10"><Hyperlink Command="{Binding MoveUp}" TextDecorations="None">🔼</Hyperlink></TextBlock>
												<TextBlock Grid.Row="1" FontSize="10"><Hyperlink Command="{Binding MoveDown}" TextDecorations="None">🔽</Hyperlink></TextBlock>
											</Grid>
											<TextBlock Grid.Column="3" Margin="10" FontSize="16">
												<Hyperlink Command="{Binding Disable}">
													<TextBlock>
														<TextBlock.Style>
															<Style>
																<Setter Property="TextBlock.Foreground" Value="Green" />
																<Setter Property="TextBlock.Text" Value="Enabled" />
																<Setter Property="FrameworkElement.ToolTip" Value="Toggle whether this browser appears in the picker" />
																<Style.Triggers>
																	<DataTrigger Binding="{Binding Model.Disabled}" Value="True">
																		<Setter Property="TextBlock.Foreground" Value="OrangeRed" />
																		<Setter Property="TextBlock.Text" Value="Disabled" />
																	</DataTrigger>
																</Style.Triggers>
															</Style>
														</TextBlock.Style>
													</TextBlock>
												</Hyperlink>
											</TextBlock>
											<TextBlock Grid.Column="4" Margin="10" FontSize="16">
												<Hyperlink Command="{Binding Edit}">
													<TextBlock Foreground="CadetBlue" Text="Edit" ToolTip="Edit browser definition" />
												</Hyperlink>
											</TextBlock>
											<TextBlock Grid.Column="5" Margin="10" FontSize="16">
												<Hyperlink Command="{Binding Duplicate}">
													<TextBlock Foreground="LightGreen" Text="+" ToolTip="Duplicate browser: create a new browser using this browser as a starting point" />
												</Hyperlink>
											</TextBlock>
											<TextBlock Grid.Column="6" Margin="10" FontSize="16">
												<TextBlock.Style>
													<Style TargetType="TextBlock">
														<Style.Triggers>
															<DataTrigger Binding="{Binding CanRemove}" Value="False">
																<Setter Property="Visibility" Value="Hidden" />
															</DataTrigger>
														</Style.Triggers>
													</Style>
												</TextBlock.Style>
												<Hyperlink Command="{Binding Remove}">
													<TextBlock Foreground="Red" Text="X" ToolTip="Remove custom browser from list" />
												</Hyperlink>
											</TextBlock>
										</Grid>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</ScrollViewer>
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Behaviour">
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<Grid.Resources>
							<Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="{x:Type CheckBox}">
								<Setter Property="Margin" Value="5,0,0,5" />
							</Style>
						</Grid.Resources>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" MinWidth="260" />
							<ColumnDefinition Width="*" MinWidth="260" />
						</Grid.ColumnDefinitions>

						<!--  Left: Appearance & list order  -->
						<StackPanel Grid.Column="0" Margin="0,0,16,0">
							<TextBlock Margin="0,4,0,6" FontWeight="Bold" Text="Appearance" />
							<StackPanel Margin="0,0,0,8" Orientation="Horizontal">
								<TextBlock VerticalAlignment="Center" Text="Theme: " />
								<ComboBox Width="120" VerticalAlignment="Center" ItemsSource="{Binding Source={StaticResource ThemeModes}}" SelectedItem="{Binding Settings.ThemeMode}" />
							</StackPanel>
							<StackPanel Margin="0,0,0,8" Orientation="Horizontal">
								<TextBlock VerticalAlignment="Center" Text="Font size: " />
								<TextBox
									MinWidth="50"
									MinHeight="28"
									Padding="6,4"
									VerticalAlignment="Center"
									VerticalContentAlignment="Center"
									Text="{Binding Settings.FontSize, UpdateSourceTrigger=PropertyChanged}" />
								<TextBlock Margin="2,0,0,0" VerticalAlignment="Center" Text=" px" />
							</StackPanel>
							<CheckBox Content="Auto size window" IsChecked="{Binding Settings.AutoSizeWindow}" ToolTip="When checked, the window fits its content. When unchecked, the window uses the last size you set by resizing. Resize the window to fix a size; that turns this off and saves the size." />
							<CheckBox Content="Turn off transparency" IsChecked="{Binding Settings.DisableTransparency}" />
							<StackPanel Margin="0,4,0,0" Orientation="Horizontal">
								<StackPanel.Style>
									<Style>
										<Setter Property="UIElement.IsEnabled" Value="True" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding Settings.DisableTransparency}" Value="True">
												<Setter Property="UIElement.IsEnabled" Value="False" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</StackPanel.Style>
								<TextBlock VerticalAlignment="Center" Text="Window opacity: " />
								<Slider
									Width="120"
									Margin="4,0"
									VerticalAlignment="Center"
									Maximum="1"
									Minimum="0.5"
									Value="{Binding Settings.WindowOpacity, UpdateSourceTrigger=Default}" />
								<TextBlock MinWidth="32" VerticalAlignment="Center" Text="{Binding Settings.WindowOpacity, StringFormat=P0}" />
							</StackPanel>

							<TextBlock Margin="0,16,0,6" FontWeight="Bold" Text="List order" />
							<RadioButton Margin="0,2" Content="Sort list manually" GroupName="OrderBy" IsChecked="{Binding Settings.UseManualOrdering}" />
							<RadioButton Margin="0,2" Content="Sort list based on usage" GroupName="OrderBy" IsChecked="{Binding Settings.UseAutomaticOrdering}" />
							<RadioButton Margin="0,2,0,6" Content="Sort list alphabetically" GroupName="OrderBy" IsChecked="{Binding Settings.UseAlphabeticalOrdering}" />

							<TextBlock Margin="0,16,0,6" FontWeight="Bold" Text="Profile display" />
							<RadioButton Margin="0,2" Content="Group profiles under browser" GroupName="ProfileDisplay" IsChecked="{Binding UseGroupedProfiles}" />
							<RadioButton Margin="0,2,0,6" Content="Show profiles as separate entries" GroupName="ProfileDisplay" IsChecked="{Binding UseFlatProfiles}" />
						</StackPanel>

						<!--  Right: Selection & defaults  -->
						<StackPanel Grid.Column="1">
							<TextBlock Margin="0,4,0,6" FontWeight="Bold" Text="When to show the picker" />
							<CheckBox Content="Always show browser selection window" IsChecked="{Binding Settings.AlwaysPrompt}" />
							<CheckBox Content="Auto close on focus lost" IsChecked="{Binding AutoCloseOnFocusLost}" ToolTip="When checked, the picker closes itself if you switch to another window. Turn this off to keep picker windows pinned until you close or use them." />
							<CheckBox Content="Always ask when no default is matching URL" IsChecked="{Binding Settings.AlwaysAskWithoutDefault}" />
							<CheckBox Content="Use defaults even when browser is not running" IsChecked="{Binding Settings.AlwaysUseDefaults}" ToolTip="When unchecked, the selection window appears if your default browser is not running. Check this to always open links in your default (or fallback) browser without showing the selection window." />

							<TextBlock Margin="0,16,0,6" FontWeight="Bold" Text="Lookup timeout" />
							<StackPanel Margin="20,0,0,0">
								<StackPanel Orientation="Horizontal">
									<TextBlock VerticalAlignment="Center" Text="URL resolution timeout: " />
									<TextBox
										MinWidth="70"
										MinHeight="28"
										Margin="4,0,0,0"
										Padding="6,4"
										VerticalAlignment="Center"
										VerticalContentAlignment="Center"
										Text="{Binding Settings.UrlLookupTimeoutMilliseconds, UpdateSourceTrigger=PropertyChanged}" />
									<TextBlock Margin="2,0,0,0" VerticalAlignment="Center" Text=" ms" />
								</StackPanel>
							</StackPanel>

							<TextBlock Margin="0,16,0,6" FontWeight="Bold" Text="When no URL rule matches" />
							<CheckBox Content="Use a fallback default browser" IsChecked="{Binding Settings.UseFallbackDefault}" ToolTip="When checked, links that match no rule open in the browser you select below. When unchecked, the picker is shown (or you are asked) depending on other settings." />
							<StackPanel Margin="20,4,0,0">
								<StackPanel.Style>
									<Style>
										<Setter Property="UIElement.IsEnabled" Value="True" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding Settings.UseFallbackDefault}" Value="False">
												<Setter Property="UIElement.IsEnabled" Value="False" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</StackPanel.Style>
								<StackPanel Orientation="Horizontal">
									<TextBlock VerticalAlignment="Center" Text="Fallback browser: " />
									<ComboBox
										MinWidth="180"
										VerticalAlignment="Center"
										d:ItemsSource="{Binding ParentViewModel.Choices}"
										IsSynchronizedWithCurrentItem="False"
										ItemContainerStyle="{StaticResource VisibleBrowserComboItemStyle}"
										ItemTemplate="{StaticResource BrowserIconAndNameCombo}"
										ItemsSource="{Binding ParentViewModel.Choices}"
										SelectedValue="{Binding Settings.DefaultBrowser}"
										SelectedValuePath="Model.Id"
										Style="{StaticResource BrowserComboBoxStyle}" />
								</StackPanel>
							</StackPanel>

						</StackPanel>
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Security">
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<Grid.Resources>
							<Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="{x:Type CheckBox}">
								<Setter Property="Margin" Value="5,0,0,5" />
							</Style>
						</Grid.Resources>
						<StackPanel>
							<Menu Margin="0,4,0,10" Background="Transparent" Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}">
								<MenuItem Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" Header="Apply profile" ItemsSource="{x:Static securityProfiles:PredefinedSecurityProfiles.All}">
									<MenuItem.ItemContainerStyle>
										<Style TargetType="{x:Type MenuItem}">
											<Setter Property="Header" Value="{Binding DisplayName}" />
											<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
											<Setter Property="IsCheckable" Value="True" />
											<Setter Property="Command" Value="{Binding DataContext.ApplySecurityProfile, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
											<Setter Property="CommandParameter" Value="{Binding}" />
											<Setter Property="IsChecked">
												<Setter.Value>
													<MultiBinding Converter="{StaticResource SecurityProfileSelectionConverter}" Mode="OneWay">
														<Binding Path="DataContext.SelectedSecurityProfile" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
														<Binding />
													</MultiBinding>
												</Setter.Value>
											</Setter>
										</Style>
									</MenuItem.ItemContainerStyle>
								</MenuItem>
							</Menu>

							<TextBlock Margin="0,4,0,6" FontWeight="Bold" Text="Automatic probes" />
							<CheckBox Content="Probe URL shorteners / redirects" IsChecked="{Binding Settings.ProbeRedirects}" />
							<StackPanel Margin="20,0,0,8">
								<StackPanel.Style>
									<Style TargetType="StackPanel">
										<Setter Property="IsEnabled" Value="True" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding Settings.ProbeRedirects}" Value="False">
												<Setter Property="IsEnabled" Value="False" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</StackPanel.Style>
								<CheckBox Content="Only for known URL shorteners" IsChecked="{Binding Settings.RedirectsKnownOnly}" />
							</StackPanel>

							<CheckBox Content="Probe favicon" IsChecked="{Binding Settings.ProbeFavicons}" />
							<StackPanel Margin="20,0,0,8">
								<StackPanel.Style>
									<Style TargetType="StackPanel">
										<Setter Property="IsEnabled" Value="True" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding Settings.ProbeFavicons}" Value="False">
												<Setter Property="IsEnabled" Value="False" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</StackPanel.Style>
								<CheckBox Content="Only for URLs matching defaults" IsChecked="{Binding Settings.FaviconsForDefaults}" />
							</StackPanel>

							<TextBlock Margin="0,8,0,6" FontWeight="Bold" Text="Manual probes" />
							<CheckBox Content="Include DNS CAA and certificate transparency evidence" IsChecked="{Binding Settings.CheckCertificateRecords}" ToolTip="Used only when you click Check connection. CAA requires a DNS lookup; CT checks embedded certificate transparency evidence." />
							<CheckBox Content="Hide connection check button" IsChecked="{Binding Settings.HideManualConnectionCheck}" ToolTip="Removes the padlock connection check action from the picker." />
							<CheckBox Content="Skip confirmation prompt" IsChecked="{Binding Settings.SkipConnectionCheckConfirmation}" ToolTip="Starts the connection check immediately when you click the padlock." />
						</StackPanel>
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Defaults">
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<TextBlock Margin="5" Text="Set default browser per domain" />
						<DataGrid
							Grid.Row="1"
							AutoGenerateColumns="False"
							Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}"
							BorderBrush="{x:Null}"
							CanUserAddRows="False"
							CanUserDeleteRows="False"
							CanUserReorderColumns="False"
							CanUserResizeColumns="False"
							ItemsSource="{Binding Defaults}"
							RowHeight="36">
							<DataGrid.RowStyle>
								<Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="DataGridRow">
									<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
									<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" />
									<Style.Triggers>
										<Trigger Property="IsSelected" Value="True">
											<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundSemiTransparentBrushKey}}" />
											<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
										</Trigger>
									</Style.Triggers>
								</Style>
							</DataGrid.RowStyle>
							<DataGrid.CellStyle>
								<Style BasedOn="{StaticResource {x:Type DataGridCell}}" TargetType="DataGridCell">
									<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
									<Setter Property="Background" Value="Transparent" />
									<Setter Property="BorderBrush" Value="Transparent" />
									<Style.Triggers>
										<Trigger Property="IsSelected" Value="True">
											<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundSemiTransparentBrushKey}}" />
											<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
											<Setter Property="BorderBrush" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
										</Trigger>
									</Style.Triggers>
								</Style>
							</DataGrid.CellStyle>
							<DataGrid.Columns>
								<DataGridTemplateColumn Width="150" Header="Match by">
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate DataType="common:DefaultSetting">
											<ComboBox
												MinWidth="130"
												IsSynchronizedWithCurrentItem="False"
												ItemsSource="{x:Static viewModels:ConfigurationViewModel.MatchTypesForRules}"
												SelectedItem="{Binding Type}"
												Style="{StaticResource DefaultsGridComboBoxStyle}" />
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
								<DataGridTextColumn Width="*" Binding="{Binding Pattern}" Header="Pattern">
									<DataGridTextColumn.ElementStyle>
										<Style TargetType="TextBlock">
											<Setter Property="VerticalAlignment" Value="Center" />
											<Setter Property="Padding" Value="5,0" />
										</Style>
									</DataGridTextColumn.ElementStyle>
									<DataGridTextColumn.EditingElementStyle>
										<Style BasedOn="{StaticResource DefaultsGridEditorTextBoxStyle}" TargetType="TextBox" />
									</DataGridTextColumn.EditingElementStyle>
								</DataGridTextColumn>
								<DataGridTemplateColumn Width="*" MinWidth="140" Header="Browser" SortMemberPath="Model.Name">
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate DataType="common:DefaultSetting">
											<ComboBox
												IsSynchronizedWithCurrentItem="False"
												ItemContainerStyle="{StaticResource VisibleBrowserComboItemStyle}"
												ItemTemplate="{StaticResource BrowserIconAndNameCombo}"
												ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}, Path=DataContext.ParentViewModel.Choices}"
												SelectedValue="{Binding Browser}"
												SelectedValuePath="Model.Id"
												Style="{StaticResource DefaultsGridComboBoxStyle}" />
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
								<DataGridTemplateColumn>
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate DataType="common:DefaultSetting">
											<Button Command="{Binding Remove}" Content="X" />
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
							</DataGrid.Columns>
						</DataGrid>

						<Grid Grid.Row="2" Margin="0,8,0,0">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<ComboBox
								MinWidth="100"
								MinHeight="28"
								VerticalContentAlignment="Center"
								ItemsSource="{x:Static viewModels:ConfigurationViewModel.MatchTypesForRules}"
								SelectedItem="{Binding NewDefaultMatchType}" />
							<TextBox
								Grid.Column="1"
								MinHeight="28"
								Margin="4,0,0,0"
								Padding="6,4"
								VerticalContentAlignment="Center"
								Text="{Binding NewDefaultPattern}" />
							<ComboBox
								Grid.Column="2"
								MinWidth="160"
								Margin="4,0,0,0"
								IsSynchronizedWithCurrentItem="False"
								ItemContainerStyle="{StaticResource VisibleBrowserComboItemStyle}"
								ItemTemplate="{StaticResource BrowserIconAndNameCombo}"
								ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=views:Configuration}, Path=DataContext.ParentViewModel.Choices}"
								SelectedValue="{Binding NewDefaultBrowser}"
								SelectedValuePath="Model.Id"
								Style="{StaticResource BrowserComboBoxStyle}" />
							<ComboBox
								Grid.Column="3"
								MinWidth="80"
								MinHeight="28"
								Margin="4,0,0,0"
								VerticalContentAlignment="Center"
								DisplayMemberPath="Name"
								ItemsSource="{Binding NewDefaultProfileChoices}"
								SelectedValue="{Binding NewDefaultProfile}"
								SelectedValuePath="Id">
								<ComboBox.Style>
									<Style BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="ComboBox">
										<Setter Property="Visibility" Value="Collapsed" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding HasNewDefaultProfileChoices}" Value="True">
												<Setter Property="Visibility" Value="Visible" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</ComboBox.Style>
							</ComboBox>
							<Button
								Grid.Column="4"
								MinHeight="28"
								Margin="4,0,0,0"
								Padding="12,0"
								VerticalAlignment="Stretch"
								Command="{Binding AddDefault}"
								Content="Add" />
						</Grid>
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Test defaults">
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<TextBlock Text="URL:" />

						<TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding TestDefaultsURL, UpdateSourceTrigger=PropertyChanged}" />

						<TextBlock Grid.Row="2" Grid.Column="0" Text="Default browser matched:" />
						<TextBlock Grid.Row="2" Grid.Column="1" Foreground="LightGreen" Text="{Binding TestDefaultsResult}" />

						<TextBlock Grid.Row="3" Grid.Column="0" Text="Actual browser choice:" />
						<TextBlock Grid.Row="3" Grid.Column="1" Foreground="LightGreen" Text="{Binding TestActualResult}" />
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem Header="Backup">
				<ScrollViewer Padding="4" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid Margin="10" VerticalAlignment="Top">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<TextBlock Margin="5" Text="Back up or restore your BrowserPicker configuration:" />
						<WrapPanel Grid.Row="1" Margin="5,5,5,0">
							<Button Margin="0,0,8,8" Padding="10,6" Command="{Binding Backup}">
								<StackPanel Orientation="Horizontal">
									<TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" Text="&#xE105;" />
									<TextBlock VerticalAlignment="Center" Text="Export Browser Picker settings" />
								</StackPanel>
							</Button>
							<Button Margin="0,0,8,8" Padding="10,6" Command="{Binding Restore}">
								<StackPanel Orientation="Horizontal">
									<TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" Text="&#xE8E5;" />
									<TextBlock VerticalAlignment="Center" Text="Import Browser Picker settings" />
								</StackPanel>
							</Button>
							<Button Margin="0,0,8,8" Padding="10,6" Command="{Binding CopySettings}">
								<StackPanel Orientation="Horizontal">
									<TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" Text="&#xE8C8;" />
									<TextBlock VerticalAlignment="Center" Text="Copy settings" />
								</StackPanel>
							</Button>
							<Button Margin="0,0,0,8" Padding="10,6" Command="{Binding PasteSettings}">
								<StackPanel Orientation="Horizontal">
									<TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" Text="&#xE77F;" />
									<TextBlock VerticalAlignment="Center" Text="Paste settings" />
								</StackPanel>
							</Button>
						</WrapPanel>
						<ScrollViewer Grid.Row="2" Grid.Column="0" Margin="5" VerticalScrollBarVisibility="Auto">
							<TextBlock Text="{Binding Settings.BackupLog}" TextWrapping="Wrap" />
						</ScrollViewer>
					</Grid>
				</ScrollViewer>
			</TabItem>

			<TabItem x:Name="FeedbackTab" Header="Feedback">
				<Grid x:Name="FeedbackContentRoot" d:DataContext="{d:DesignInstance viewModels:FeedbackViewModel, d:IsDesignTimeCreatable=True}">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<Border Padding="12" Background="{DynamicResource {x:Static ui:App.ContentBackgroundSemiTransparentBrushKey}}">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>
							<StackPanel>
								<TextBlock FontSize="18" FontWeight="SemiBold" Text="Feedback and diagnostics" />
								<TextBlock Margin="0,6,0,0" TextWrapping="Wrap">
									When something feels off, copy a ready-made report for GitHub or open a new issue with the important context already filled in.
								</TextBlock>
							</StackPanel>
							<WrapPanel Grid.Row="1" Margin="0,12,0,0">
								<Button Margin="0,0,8,8" Padding="12,4" Command="{Binding CopyFeedbackMarkdown}" Content="Copy feedback report as Markdown" />
								<Button Margin="0,0,8,8" Padding="12,4" Command="{Binding ReportBug}" Content="Report bug on GitHub" />
								<Button Margin="0,0,0,8" Padding="12,4" Command="{Binding CopyApplicationLog}" Content="Copy raw application log" />
							</WrapPanel>
						</Grid>
					</Border>
					<Border Grid.Row="1" Background="#111827" SnapsToDevicePixels="True">
						<Grid Margin="0">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<Grid Background="#1F2937">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>
								<StackPanel Margin="12,10" VerticalAlignment="Center" Orientation="Horizontal">
									<TextBlock FontWeight="SemiBold" Foreground="#F9FAFB" Text="Application log" />
									<TextBlock Text="  " />
									<TextBlock FontSize="11" Foreground="#9CA3AF" Text="{Binding ApplicationLogStatus}" />
								</StackPanel>
								<Border
									Grid.Column="1"
									Margin="8,10,12,10"
									Padding="8,2"
									VerticalAlignment="Center"
									Background="#374151"
									CornerRadius="999">
									<TextBlock FontSize="11" Foreground="#FDE68A" Text="{Binding ApplicationLogEntryCount, StringFormat={}{0} lines}" />
								</Border>
							</Grid>
							<Grid Grid.Row="1" Background="#162132">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<TextBlock
									Margin="12,10,8,10"
									VerticalAlignment="Center"
									FontSize="11"
									Foreground="#94A3B8"
									Text="Level" />
								<ComboBox
									Grid.Column="1"
									MinWidth="150"
									MinHeight="32"
									Margin="0,8,12,8"
									VerticalAlignment="Center"
									VerticalContentAlignment="Center"
									DisplayMemberPath="Label"
									ItemsSource="{x:Static viewModels:ApplicationLogLevelOptions.All}"
									SelectedItem="{Binding SelectedApplicationLogLevelOption}" />
								<TextBlock
									Grid.Column="2"
									Margin="0,10,8,10"
									VerticalAlignment="Center"
									FontSize="11"
									Foreground="#94A3B8"
									Text="Filter" />
								<Grid Grid.Column="3" Margin="0,8,12,8">
									<TextBox
										Padding="8,4,26,4"
										Background="#0F172A"
										BorderBrush="#334155"
										CaretBrush="#F59E0B"
										Foreground="#E5E7EB"
										Text="{Binding ApplicationLogFilter, UpdateSourceTrigger=PropertyChanged}"
										ToolTip="Filter on timestamp, level, context, event, and message." />
									<TextBlock
										Margin="10,0,28,0"
										VerticalAlignment="Center"
										Foreground="#64748B"
										IsHitTestVisible="False"
										Text="Filter timestamp, level, context, event, or message">
										<TextBlock.Style>
											<Style TargetType="TextBlock">
												<Setter Property="Visibility" Value="Collapsed" />
												<Style.Triggers>
													<DataTrigger Binding="{Binding HasApplicationLogFilter}" Value="False">
														<Setter Property="Visibility" Value="Visible" />
													</DataTrigger>
												</Style.Triggers>
											</Style>
										</TextBlock.Style>
									</TextBlock>
									<Button
										Width="18"
										Height="18"
										Margin="0,0,6,0"
										Padding="0"
										HorizontalAlignment="Right"
										VerticalAlignment="Center"
										Background="#334155"
										BorderBrush="#475569"
										Command="{Binding ClearApplicationLogFilter}"
										Content="×"
										Foreground="#E5E7EB">
										<Button.Style>
											<Style TargetType="Button">
												<Setter Property="Visibility" Value="Collapsed" />
												<Style.Triggers>
													<DataTrigger Binding="{Binding HasApplicationLogFilter}" Value="True">
														<Setter Property="Visibility" Value="Visible" />
													</DataTrigger>
												</Style.Triggers>
											</Style>
										</Button.Style>
									</Button>
								</Grid>
							</Grid>
							<Grid Grid.Row="2" Grid.IsSharedSizeScope="True">
								<ListBox
									Padding="0"
									ui:AutoTailListBoxBehavior.IsEnabled="True"
									Background="#111827"
									BorderThickness="0"
									Foreground="#E5E7EB"
									ItemsSource="{Binding FilteredApplicationLogEntries}"
									ScrollViewer.CanContentScroll="True"
									ScrollViewer.HorizontalScrollBarVisibility="Disabled"
									ScrollViewer.VerticalScrollBarVisibility="Auto"
									VirtualizingPanel.IsVirtualizing="True"
									VirtualizingPanel.ScrollUnit="Pixel"
									VirtualizingPanel.VirtualizationMode="Recycling">
									<ListBox.ItemContainerStyle>
										<Style TargetType="ListBoxItem">
											<Setter Property="HorizontalContentAlignment" Value="Stretch" />
											<Setter Property="Padding" Value="0" />
											<Setter Property="Margin" Value="0" />
											<Setter Property="BorderThickness" Value="0" />
											<Setter Property="Background" Value="Transparent" />
											<Setter Property="Focusable" Value="False" />
										</Style>
									</ListBox.ItemContainerStyle>
									<ListBox.ItemTemplate>
										<DataTemplate DataType="{x:Type common:InMemoryLogEntry}">
											<Border Padding="12,8" BorderBrush="#1F2937" BorderThickness="0,0,0,1">
												<Grid>
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="Auto" MinWidth="240" MaxWidth="360" SharedSizeGroup="LogMetadataColumn" />
														<ColumnDefinition Width="*" />
													</Grid.ColumnDefinitions>
													<StackPanel Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Top">
														<StackPanel Orientation="Horizontal">
															<TextBlock
																Margin="0,0,10,0"
																VerticalAlignment="Center"
																FontFamily="Cascadia Mono,Consolas"
																FontSize="12"
																Foreground="#94A3B8"
																Text="{Binding TimestampDisplay}" />
															<Border Margin="0,0,10,0" Padding="8,2" VerticalAlignment="Center" CornerRadius="999">
																<Border.Style>
																	<Style TargetType="Border">
																		<Setter Property="Background" Value="#334155" />
																		<Style.Triggers>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Trace}">
																				<Setter Property="Background" Value="#4B5563" />
																			</DataTrigger>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Debug}">
																				<Setter Property="Background" Value="#1D4ED8" />
																			</DataTrigger>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Information}">
																				<Setter Property="Background" Value="#047857" />
																			</DataTrigger>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Warning}">
																				<Setter Property="Background" Value="#B45309" />
																			</DataTrigger>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Error}">
																				<Setter Property="Background" Value="#B91C1C" />
																			</DataTrigger>
																			<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Critical}">
																				<Setter Property="Background" Value="#7C3AED" />
																			</DataTrigger>
																		</Style.Triggers>
																	</Style>
																</Border.Style>
																<TextBlock
																	FontFamily="Cascadia Mono,Consolas"
																	FontSize="11"
																	FontWeight="SemiBold"
																	Foreground="White"
																	Text="{Binding LevelDisplay}" />
															</Border>
															<TextBlock
																VerticalAlignment="Center"
																FontSize="12"
																Foreground="#60A5FA"
																Text="{Binding CategoryDisplay}"
																TextTrimming="CharacterEllipsis" />
														</StackPanel>
														<TextBlock
															Margin="0,4,0,0"
															FontFamily="Cascadia Mono,Consolas"
															FontSize="11"
															Foreground="#94A3B8"
															Text="{Binding EventDisplay}"
															TextTrimming="CharacterEllipsis" />
													</StackPanel>
													<TextBlock
														Grid.Column="1"
														Margin="0,1,0,0"
														VerticalAlignment="Top"
														ui:TextBlockInlineSegments.Segments="{Binding Segments}"
														FontFamily="Cascadia Mono,Consolas"
														FontSize="12"
														TextWrapping="Wrap">
														<TextBlock.Style>
															<Style TargetType="TextBlock">
																<Setter Property="Foreground" Value="#E5E7EB" />
																<Style.Triggers>
																	<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Warning}">
																		<Setter Property="Foreground" Value="#FDE68A" />
																	</DataTrigger>
																	<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Error}">
																		<Setter Property="Foreground" Value="#FCA5A5" />
																	</DataTrigger>
																	<DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Critical}">
																		<Setter Property="Foreground" Value="#DDD6FE" />
																	</DataTrigger>
																</Style.Triggers>
															</Style>
														</TextBlock.Style>
													</TextBlock>
												</Grid>
											</Border>
										</DataTemplate>
									</ListBox.ItemTemplate>
								</ListBox>
								<TextBlock
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									FontStyle="Italic"
									Foreground="#94A3B8"
									Text="Logs will appear here as Browser Picker runs.">
									<TextBlock.Style>
										<Style TargetType="TextBlock">
											<Setter Property="Visibility" Value="Collapsed" />
											<Style.Triggers>
												<DataTrigger Binding="{Binding VisibleApplicationLogEntryCount}" Value="0">
													<Setter Property="Visibility" Value="Visible" />
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</Grid>
						</Grid>
					</Border>
				</Grid>
			</TabItem>

			<TabItem Header="URL shorteners">
				<Grid Margin="4">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<TextBlock Grid.ColumnSpan="2" Margin="0,0,0,8" Text="These domains are known URL shorteners and get probed on launch unless disallowed by settings." TextWrapping="Wrap" />
					<TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,0,4" Text="Default shorteners" />
					<TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,4" Text="Additional shorteners" />
					<Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" MinWidth="120" />
							<ColumnDefinition Width="8" />
							<ColumnDefinition Width="*" MinWidth="120" />
						</Grid.ColumnDefinitions>
						<ListBox
							Grid.Column="0"
							ItemsSource="{Binding DefaultUrlShorteners}"
							ScrollViewer.HorizontalScrollBarVisibility="Disabled"
							ScrollViewer.VerticalScrollBarVisibility="Auto"
							SelectionMode="Single" />
						<ListBox
							x:Name="UrlShorteners"
							Grid.Column="2"
							ItemsSource="{Binding AdditionalUrlShorteners}"
							ScrollViewer.HorizontalScrollBarVisibility="Disabled"
							ScrollViewer.VerticalScrollBarVisibility="Auto"
							SelectionMode="Single" />
					</Grid>
					<TextBlock
						Grid.Row="3"
						Grid.Column="0"
						Margin="0,0,8,0"
						VerticalAlignment="Center"
						FontSize="10">
						<TextBlock.Text>ℹ️ Some additional jump pages not shown above</TextBlock.Text>
						<TextBlock.ToolTip>
							<ToolTip>Some pages, such as outlook safelinks, have special handling where the target url is in the query string; these are not listed here as they require additional information in code.</ToolTip>
						</TextBlock.ToolTip>
					</TextBlock>
					<Grid Grid.Row="3" Grid.Column="1">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<TextBox x:Name="NewShortener" Margin="0,0,4,0" Text="{Binding NewUrlShortener}" />
						<Button
							Grid.Column="1"
							Padding="10,0"
							Command="{Binding AddShortener}"
							CommandParameter="{Binding Text, ElementName=NewShortener}"
							Content="Add" />
						<Button
							Grid.Column="2"
							Padding="10,0"
							Command="{Binding RemoveShortener}"
							CommandParameter="{Binding SelectedItem, ElementName=UrlShorteners}"
							Content="Remove" />
					</Grid>
				</Grid>
			</TabItem>

		</TabControl>

		<Grid Grid.Row="1" Margin="18,0,18,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Orientation="Horizontal">
				<StackPanel.Style>
					<Style TargetType="StackPanel">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<TextBlock Padding="4">
					<Hyperlink Command="{Binding AddBrowser}">
						<TextBlock Text="Add browser" />
					</Hyperlink>
				</TextBlock>
				<TextBlock Margin="10,0,0,0" Padding="4">
					<Hyperlink Command="{Binding RefreshBrowsers}">
						<TextBlock Text="Refresh browser list" />
					</Hyperlink>
				</TextBlock>
			</StackPanel>

			<StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
				<TextBlock Padding="4">
					<TextBlock.Style>
						<Style TargetType="TextBlock">
							<Style.Triggers>
								<DataTrigger Binding="{Binding ParentViewModel.Url.TargetURL}" Value="{x:Null}">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
					<Hyperlink Command="{Binding ParentViewModel.Configure}">
						<TextBlock Text="Done" />
					</Hyperlink>
				</TextBlock>

				<TextBlock Padding="4">
					<TextBlock.Style>
						<Style TargetType="TextBlock">
							<Setter Property="Visibility" Value="Collapsed" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding ParentViewModel.Url.TargetURL}" Value="{x:Null}">
									<Setter Property="Visibility" Value="Visible" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
					<Hyperlink Command="{Binding ParentViewModel.Exit}">
						<TextBlock Text="Done" />
					</Hyperlink>
				</TextBlock>
			</StackPanel>
		</Grid>

	</Grid>
</UserControl>
