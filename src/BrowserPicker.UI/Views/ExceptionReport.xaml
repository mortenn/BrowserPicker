<Window
	x:Class="BrowserPicker.UI.Views.ExceptionReport"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:system="clr-namespace:System;assembly=mscorlib"
	xmlns:viewModels="clr-namespace:BrowserPicker.UI.ViewModels"
	Title="An unhandled exception has occurred"
	d:DataContext="{d:DesignInstance viewModels:ExceptionViewModel,
									 d:IsDesignTimeCreatable=True}"
	SizeToContent="WidthAndHeight"
	mc:Ignorable="d">
	<Window.Resources>
		<ResourceDictionary>
			<DataTemplate DataType="{x:Type system:Exception}">
				<Border>
					<StackPanel>
						<StackPanel Orientation="Vertical">
							<StackPanel Orientation="Horizontal">
								<ToggleButton
									x:Name="toggleButton"
									Width="15"
									Height="15"
									Margin="0,0,2,0"
									Padding="2,0"
									FontSize="10">
									<ToggleButton.Style>
										<Style TargetType="ToggleButton">
											<Setter Property="Content" Value="+" />
											<Style.Triggers>
												<DataTrigger Binding="{Binding InnerException}" Value="{x:Null}">
													<Setter Property="UIElement.Visibility" Value="Collapsed" />
												</DataTrigger>
												<DataTrigger Binding="{Binding ElementName=toggleButton, Path=IsChecked}" Value="True">
													<Setter Property="Content" Value="-" />
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</ToggleButton.Style>
								</ToggleButton>
								<TextBlock Text="{Binding Message}" />
							</StackPanel>
							<TextBlock Text="{Binding StackTrace}" />
						</StackPanel>
						<Grid>
							<Grid.Style>
								<Style TargetType="Grid">
									<Style.Triggers>
										<DataTrigger Binding="{Binding InnerException}" Value="{x:Null}">
											<Setter Property="UIElement.Visibility" Value="Collapsed" />
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</Grid.Style>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>
							<Border Grid.Column="1" BorderBrush="Silver" BorderThickness="1">
								<Border.Style>
									<Style TargetType="Border">
										<Setter Property="Visibility" Value="Collapsed" />
										<Style.Triggers>
											<DataTrigger Binding="{Binding ElementName=toggleButton, Path=IsChecked}" Value="True">
												<Setter Property="UIElement.Visibility" Value="Visible" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</Border.Style>
								<ContentPresenter Content="{Binding InnerException}" />
							</Border>
						</Grid>
					</StackPanel>
				</Border>
			</DataTemplate>
		</ResourceDictionary>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<StackPanel Orientation="Vertical">
			<TextBlock Text="Unfortunately, BrowserPicker has crashed." />
			<TextBlock Text="Please submit a bug report on https://github.com/mortenn/BrowserPicker." />
			<TextBlock Margin="0,4,0,0" Text="Exception details:" />
		</StackPanel>
		<Border Grid.Row="1" Margin="5">
			<ContentPresenter Content="{Binding Model.Exception}" />
		</Border>
		<StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal">
			<Button Margin="4" Padding="10,0" Command="{Binding CopyToClipboard}" Content="Copy to clipboard" />
			<Button Margin="4" Padding="10,0" Command="{Binding Ok}" Content="Ok" />
		</StackPanel>
	</Grid>
</Window>
