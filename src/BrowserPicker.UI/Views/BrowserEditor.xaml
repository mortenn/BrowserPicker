<Window
	x:Class="BrowserPicker.UI.Views.BrowserEditor"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:BrowserPicker.UI.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ui="clr-namespace:BrowserPicker.UI"
	xmlns:viewModels="clr-namespace:BrowserPicker.UI.ViewModels"
	Title="Browser Picker"
	Width="580"
	MinWidth="580"
	d:DataContext="{d:DesignInstance viewModels:BrowserViewModel,
									 d:IsDesignTimeCreatable=true}"
	AllowsTransparency="True"
	Background="Transparent"
	ResizeMode="NoResize"
	ShowInTaskbar="False"
	SizeToContent="Height"
	Topmost="True"
	WindowStartupLocation="CenterScreen"
	WindowStyle="None"
	mc:Ignorable="d">

	<Window.Resources>
		<ResourceDictionary>
			<converters:IconFileToImageConverter x:Key="IconConverter" />
			<Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
			</Style>
			<Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="Background" Value="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" />
				<Setter Property="BorderBrush" Value="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}" />
				<Setter Property="VerticalContentAlignment" Value="Center" />
				<Setter Property="MinHeight" Value="28" />
				<Setter Property="Padding" Value="6,4" />
				<Setter Property="TextWrapping" Value="NoWrap" />
			</Style>
			<Style x:Key="EllipsisTextBox" BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
				<Style.Triggers>
					<Trigger Property="IsKeyboardFocusWithin" Value="False">
						<Setter Property="Foreground" Value="Transparent" />
					</Trigger>
				</Style.Triggers>
			</Style>
			<Style x:Key="EllipsisOverlayText" BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
				<Setter Property="Margin" Value="8,0" />
				<Setter Property="VerticalAlignment" Value="Center" />
				<Setter Property="TextWrapping" Value="NoWrap" />
				<Setter Property="TextTrimming" Value="CharacterEllipsis" />
				<Setter Property="IsHitTestVisible" Value="False" />
			</Style>
		</ResourceDictionary>
	</Window.Resources>
	<Border
		MinWidth="400"
		Padding="10"
		Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}"
		BorderBrush="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}"
		BorderThickness="2"
		CornerRadius="5"
		MouseLeftButtonDown="DragWindow">
		<Grid TextElement.Foreground="{DynamicResource {x:Static ui:App.ContentForegroundBrushKey}}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="56" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition />
				<ColumnDefinition Width="44" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock
				Grid.Row="0"
				Grid.Column="1"
				Grid.ColumnSpan="3"
				Margin="5"
				HorizontalAlignment="Center"
				Text="Add a browser" />

			<Image
				Grid.Row="0"
				Grid.RowSpan="3"
				Grid.Column="0"
				Width="40"
				Height="40"
				Margin="4,4,8,0"
				HorizontalAlignment="Left"
				VerticalAlignment="Top"
				Source="{Binding Model.IconPath, Converter={StaticResource IconConverter}}" />

			<TextBlock
				Grid.Row="1"
				Grid.Column="1"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Name: " />
			<TextBox Grid.Row="1" Grid.Column="2" Margin="2" Text="{Binding Model.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock
				Grid.Row="2"
				Grid.Column="1"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Command: " />
			<Grid Grid.Row="2" Grid.Column="2" Margin="2">
				<TextBox x:Name="CommandTextBox" Style="{StaticResource EllipsisTextBox}" Text="{Binding Model.Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Model.Command}" />
				<TextBlock Text="{Binding Text, ElementName=CommandTextBox}">
					<TextBlock.Style>
						<Style BasedOn="{StaticResource EllipsisOverlayText}" TargetType="TextBlock">
							<Style.Triggers>
								<DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=CommandTextBox}" Value="True">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
				</TextBlock>
			</Grid>
			<Button
				Grid.Row="2"
				Grid.Column="3"
				Width="36"
				Height="28"
				Margin="2"
				HorizontalAlignment="Center"
				Click="Command_Browse"
				Content="..."
				ToolTip="Browse" />

			<TextBlock
				Grid.Row="3"
				Grid.Column="0"
				Grid.ColumnSpan="2"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Command Args: " />
			<Grid Grid.Row="3" Grid.Column="2" Margin="2">
				<TextBox x:Name="CommandArgsTextBox" Style="{StaticResource EllipsisTextBox}" Text="{Binding Model.CommandArgs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Model.CommandArgs}" />
				<TextBlock Text="{Binding Text, ElementName=CommandArgsTextBox}">
					<TextBlock.Style>
						<Style BasedOn="{StaticResource EllipsisOverlayText}" TargetType="TextBlock">
							<Style.Triggers>
								<DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=CommandArgsTextBox}" Value="True">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
				</TextBlock>
			</Grid>

			<TextBlock
				Grid.Row="4"
				Grid.Column="1"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Icon: " />
			<Grid Grid.Row="4" Grid.Column="2" Margin="2">
				<TextBox x:Name="IconPathTextBox" Style="{StaticResource EllipsisTextBox}" Text="{Binding Model.IconPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Model.IconPath}" />
				<TextBlock Text="{Binding Text, ElementName=IconPathTextBox}">
					<TextBlock.Style>
						<Style BasedOn="{StaticResource EllipsisOverlayText}" TargetType="TextBlock">
							<Style.Triggers>
								<DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=IconPathTextBox}" Value="True">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
				</TextBlock>
			</Grid>
			<Button
				Grid.Row="4"
				Grid.Column="3"
				Width="36"
				Height="28"
				Margin="2"
				HorizontalAlignment="Center"
				Click="Icon_Browse"
				Content="..."
				ToolTip="Browse" />

			<CheckBox
				Grid.Row="5"
				Grid.Column="2"
				Margin="2,4"
				Content="Expand file:// URLs"
				IsChecked="{Binding Model.ExpandFileUrls}" />

			<TextBlock
				Grid.Row="6"
				Grid.Column="0"
				Grid.ColumnSpan="2"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Privacy Args: " />
			<TextBox Grid.Row="6" Grid.Column="2" Margin="2" Text="{Binding Model.PrivacyArgs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

			<CheckBox
				Grid.Row="7"
				Grid.Column="2"
				Margin="2,4"
				Content="Disable automatic updates"
				IsChecked="{Binding Model.ManualOverride}">
				<CheckBox.Style>
					<Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
						<Style.Triggers>
							<DataTrigger Binding="{Binding IsWellKnown}" Value="False">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</CheckBox.Style>
			</CheckBox>

			<TextBlock
				Grid.Row="8"
				Grid.Column="0"
				Grid.ColumnSpan="2"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				Text="Hotkey:" />
			<Grid Grid.Row="8" Grid.Column="2">
				<TextBox Margin="2" KeyDown="OnCustomKeyBindDown" KeyUp="OnCustomKeyBindUp" Text="{Binding Model.CustomKeyBind, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
				<TextBlock
					Margin="5,0"
					VerticalAlignment="Center"
					Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
					IsHitTestVisible="False"
					Text="(Automatic)">
					<TextBlock.Style>
						<Style>
							<Setter Property="UIElement.Visibility" Value="Collapsed" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding Model.CustomKeyBind}" Value="">
									<Setter Property="UIElement.Visibility" Value="Visible" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBlock.Style>
				</TextBlock>
			</Grid>

			<!--  Firefox containers opt-in  -->
			<StackPanel Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="4" Margin="2,8,2,0">
				<StackPanel.Style>
					<Style TargetType="StackPanel">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding SupportsContainers}" Value="True">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<CheckBox Margin="0,2" IsChecked="{Binding Model.ContainersEnabled}">
					<TextBlock TextWrapping="Wrap">Enable Firefox container support</TextBlock>
				</CheckBox>
				<TextBlock Margin="20,4,0,0" FontSize="11" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" TextWrapping="Wrap">
					<Run Text="Requires" />
					<Hyperlink Command="{Binding OpenExtensionLink}">Open URL in Container</Hyperlink>
					<Run Text="extension." />
				</TextBlock>
			</StackPanel>

			<!--  Profiles section  -->
			<StackPanel Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="4" Margin="2,8,2,0">
				<StackPanel.Style>
					<Style TargetType="StackPanel">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding HasProfiles}" Value="True">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<TextBlock Margin="0,0,0,4" FontWeight="Bold" Text="Profiles" />
				<ItemsControl MaxHeight="200" ItemsSource="{Binding Model.Profiles}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border Margin="0,1" Padding="4,2" Background="{DynamicResource {x:Static ui:App.ContentBackgroundBrushKey}}" CornerRadius="3">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="*" />
										<ColumnDefinition Width="Auto" />
									</Grid.ColumnDefinitions>
									<TextBlock VerticalAlignment="Center" Text="{Binding Name}" />
									<TextBlock
										Grid.Column="1"
										Margin="8,0,0,0"
										VerticalAlignment="Center"
										FontSize="11"
										Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}">
										<TextBlock.Style>
											<Style TargetType="TextBlock">
												<Setter Property="Text" Value="{Binding CommandArgs}" />
												<Style.Triggers>
													<DataTrigger Binding="{Binding CommandArgs}" Value="{x:Null}">
														<Setter Property="Text" Value="{Binding UrlTemplate}" />
													</DataTrigger>
												</Style.Triggers>
											</Style>
										</TextBlock.Style>
									</TextBlock>
								</Grid>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</StackPanel>

			<StackPanel
				Grid.Row="11"
				Grid.Column="0"
				Grid.ColumnSpan="4"
				Margin="0,5,2,5"
				HorizontalAlignment="Right"
				Orientation="Horizontal">
				<Button Padding="10,0" Click="Cancel_OnClick" Content="Cancel" />
				<Button Margin="10,0,0,0" Padding="10,0" Click="Ok_OnClick" Content="Ok" />
			</StackPanel>
		</Grid>
	</Border>
</Window>
