name: PR Validation

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  validate:
    permissions:
      contents: read
    uses: mortenn/BrowserPicker.Actions/.github/workflows/pr-validation.yml@d6c7563889c25c3df4ab20c3535fea675d6e9d12
    with:
      dotnet_version: 10.x
