name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: mortenn/BrowserPicker.Actions/.github/workflows/release.yml@b2803f3abd28e96a397fcd8cab6996797cea5b01
    with:
      dotnet_version: 10.x
